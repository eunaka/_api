/* ----------------------------------------
 * ANGULAR APP NAME IN CODEIGNITER
 * ----------------------------------------
 * The module created here is called in the
 * includes.php, in the last line. To change
 * the app name, that line must be changed
 * too.
 */
var app = angular.module('app', ['ngRoute']);